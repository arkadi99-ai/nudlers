# נודלרס - הצעדים הבאים (אחרי הסשן של 2026-09-06)

כל שבעת הפרויקטים של "מפת הדרכים לגאונות" בנויים ונבדקו מול נתונים אמיתיים. הדבר היחיד שנשאר לעשות דורש את הטלפון שלך, ולא ניתן לביצוע מרחוק.

## מה כבר עובד עכשיו (אין צורך לעשות כלום)

- **דשבורד ראשי**: מצב חשבון, תחזית "בטוח להשקיע", יעדי חיסכון, "טעון בירור".
- **מסך "מקורות ושימושים"**: דוח חודשי מלא, בלשונית נפרדת.
- **לשונית "תובנות"**: כולל שני מזהים חדשים (מגמת עלייה, מנוי אפשרי שנשכח) שנוספו לתוך המערכת הקיימת.
- **תיוג בעלים**: במסך "מקורות פיננסיים", עמודת "בעלים" (אני/אישתי/משותף).

## מה שדורש ממך פעולה - כדי להפעיל את הסוכן בוואטסאפ

1. **לחבר מחדש את הוואטסאפ** - החיבור התנתק (session ישן). דרך מסך ההגדרות באפליקציה, לסרוק קוד QR מחדש עם הטלפון.
2. **להפעיל את `whatsapp_enabled`** - הגדרה שכרגע כבויה (`false`).
3. **ליצור קבוצת וואטסאפ משפחתית** - אתה, אישתך, ולהוסיף את מספר הבוט אליה.
4. **למצוא את מזהה הקבוצה (JID)** - אחרי שהבוט חבר בקבוצה, קריאה ל-`GET /api/whatsapp/groups` תחזיר רשימה של כל הקבוצות שהבוט חבר בהן, עם שם ומזהה. תגיד לי ואני אעזור לך לזהות את הקבוצה הנכונה ולשמור אותה.
5. **בדיקה** - קריאה ל-`POST /api/whatsapp/social-agent-test` עם `{"send":true}` תשלח הודעת בדיקה אמיתית לקבוצה, כדי לוודא שהכל עובד לפני שמחכים ליום ראשון/לתחילת החודש.

**חשוב:** שלב 1 (סריקת QR) ושלב 3 (יצירת קבוצה) חייבים להתבצע על ידך בטלפון - זה לא משהו שאני יכול לעשות מרחוק. בסשן הבא נעבור על זה יחד שלב-אחר-שלב.

## איך הסוכן יתנהג ברגע שיופעל

- **עדכון שבועי** (ימי ראשון) ו**חודשי** (ה-1 לחודש), בסביבות השעה 9:00 - הודעה חמה וכיפית שמתייחסת ליתרה, לתחזית, ליעדי החיסכון, ולהוצאות של כל אחד מכם (אם תייגתם "בעלים" לכרטיסים).
- **שאלת סיווג** - אחת ל-3 ימים לכל היותר (לא מציק), הסוכן ישאל בקבוצה על ההוצאה הכי גדולה שלא סווגה. **תגובה בציטוט (reply) על ההודעה שלו** תגרום לו לסווג את זה אוטומטית ולאשר בקבוצה. תגובה לכל הודעה אחרת בקבוצה מתעלמת - הוא לא "מקשיב" לשיחה הרגילה שלכם.

## הערה כללית

תוודא שהכל נראה נכון אצלך בדפדפן - בסשן הזה עדיין אין לי אפשרות לבדוק ויזואלית.
